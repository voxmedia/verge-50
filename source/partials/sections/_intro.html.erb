<li class="m-pages__page page" id="<%= page.slug %>" data-page-url="<%= page_url(page.slug) %>" data-name="<%= page.title %>">
  <div class="l-content-wrap">
    <article class="m-intro">
      <div class="m-intro__content">
        <h2>The Fifty</h2>
        <%= data.pages.find{ |p| p.name == 'intro' }.content %>
      </div>
    </article>
  </div>
</li>
